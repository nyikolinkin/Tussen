import 'package:flutter/material.dart';

class TussenLoading extends StatelessWidget {
  const TussenLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}